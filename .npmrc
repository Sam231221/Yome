# Prefer workspace hoisting compatible with Turborepo and Bun
# https://turbo.build/repo/docs/handbook/package-installation
shamefully-hoist=false

# Use exact versions when adding deps (reduces drift)
save-exact=false

# Prefer bun for installs when using this repo
engine-strict=true
